# XS-2026-0006 任务书 CLI 命令未完整实现

- 标题：早期执行计划把九个 CLI 命令错误缩减为三个只读命令
- 严重级别：P1
- 发现时间：2026-08-02 UTC
- 环境：Linux Agent、双节点 network namespace 与本地 Unix IPC
- 版本和提交：远端 `46181c9` 及之前版本
- 前置条件：Agent 已注册并建立 Controller 与对等节点会话
- 复现步骤：执行任务书要求的 `xs ping`、`xs path`、`xs routes`、`xs netcheck`、`xs reconnect` 或 `xs version`
- 期望：九个命令全部存在；认证探测、路径、路由、健康、重连和版本均返回真实运行时结果，失败有非零退出码
- 实际：仅有 `status`、`peers`、`diagnostics` 和全局 `--version`
- 日志：`/srv/xs-nexus/artifacts/qa/m1.2-cli-completion-20260802T100106Z`
- 截图：不适用
- 根因：M1.2 执行计划错误缩减了任务书清单，后续回归复用了不完整验收范围
- 修复：增加完整有界 IPC 合约；`ping` 复用认证 XSP/1 PathChallenge/PathResponse；`path`、`routes`、`netcheck` 返回运行时事实；`reconnect` 有一秒冷却、确认响应和有界 channel；`version` 与 `--version` 一致
- 回归测试：Core/Agent/CLI 单测、严格 Clippy、真实 PostgreSQL Agent 控制和双 Agent namespace 九命令全链路通过；失败 ping、非健康 netcheck 和拒绝重连返回非零
- 相关提交：远端 `e272114`（`feat(cli): complete local agent diagnostics`）
- 状态：已关闭
