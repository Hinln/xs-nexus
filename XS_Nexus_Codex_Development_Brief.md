# Codex 总任务书：从零开发“拾枢（XS Nexus）”自研内网互通系统

> 工作目录当前为空。请直接在当前目录初始化并实施整个项目。  
> 项目版权归属：四川夏拾网络科技有限责任公司。  
> 项目暂定名称：**拾枢（XS Nexus）**。  
> 管理控制台计划域名：`https://vpn.xiashikeji.cn`。  
> 默认虚拟网络地址池：`100.88.0.0/16`，必须支持后台修改并检测本地网段冲突。

---

## 一、项目目标

从零开发一套由我方完全掌控的安全内网互通系统，用于连接：

- Windows 10 / Windows 11 电脑；
- Linux 服务器；
- NAS 设备；
- 不同云厂商、不同公网出口、不同局域网中的设备；
- 无公网 IP、位于 NAT 或防火墙后的设备；
- 后续可扩展 macOS、Android、iOS 和路由器系统。

系统需要实现：

1. 设备加入同一个虚拟三层网络后，可通过虚拟 IP 互相访问；
2. 优先建立设备间 UDP 点对点直连；
3. 无法直连时自动切换到我方自研中继节点；
4. 支持把真实局域网网段发布到虚拟网络，实现站点到站点互通；
5. 支持中央控制台管理设备、地址、路由、权限、密钥和审计日志；
6. 支持一键安装、注册、升级、卸载和故障诊断；
7. 控制平面集中管理，但正常业务数据尽量不经过中心服务器；
8. 即使控制器短暂离线，已经建立的节点连接仍应继续工作；
9. 所有数据流量必须端到端加密，中继服务器不得读取业务明文；
10. 最终形成可实际部署、可持续维护的完整产品，不是界面原型或概念验证页面。

---

## 二、“完全自研”的强制边界

### 2.1 禁止事项

项目不得在运行时、构建产物或安装包中依赖、调用、封装、二次发行或改名使用以下现成组网核心：

- Tailscale；
- Headscale；
- WireGuard 可执行程序或 WireGuard 内核模块；
- ZeroTier；
- NetBird；
- Nebula；
- OpenVPN；
- SoftEther；
- FRP、rathole、nps 等穿透工具；
- coturn 或其他现成 STUN/TURN 服务端；
- TAP-Windows、OpenVPN TAP 驱动；
- 任何通过启动第三方进程来完成隧道、路由、NAT 穿透或中继的方案。

不得：

- 从上述项目复制、翻译、改写或拼接源代码；
- 直接复刻其私有协议、数据包格式、密钥格式或品牌标识；
- 用现成项目先跑通，再在界面上包装成“自研”；
- 通过 Shell 调用第三方组网命令完成核心能力；
- 为了快速通过测试而关闭加密、证书校验、权限检查或驱动签名检查；
- 自创未经审计的密码算法。

项目所有者批准一个严格受限的 Windows 发行例外：首版可固定使用发行方签名的官方 Wintun `0.14.1` x64 DLL，但只能作为 `xs-windows-wintun` 的 L3 虚拟网卡适配器。Wintun 不得实现或替代注册、身份、XSP/1、密钥协商、加密、重放保护、ACL、路由授权、NAT 穿透、候选路径或 Relay；自研 `drivers/windows-xsnet` 源码继续完全独立，且不得依赖 Wintun。

### 2.2 允许事项

允许使用：

- 操作系统原生能力，例如 Linux `/dev/net/tun`、Netlink、Windows WDK、NetAdapterCx、Windows 路由和加密 API；
- IETF RFC、操作系统官方文档和公开论文；
- 编程语言、编译器、通用 Web 框架、数据库驱动、序列化库、日志库和测试框架；
- 经过广泛验证的标准密码学原语及成熟密码库；
- PostgreSQL 等通用基础设施；
- GitHub 成熟项目的公开架构思想、故障案例和测试方法。

“完全自研”在本项目中的定义是：

> **控制平面、节点代理、虚拟网卡适配、组网协议、地址管理、路由管理、NAT 穿透、中继协议、策略引擎、管理控制台、安装器和升级系统均由本项目独立设计与实现；可以使用通用依赖和标准密码学库，但不能依赖任何现成组网产品完成核心功能。**

### 2.3 Clean-room 要求

创建并持续维护：

- `docs/REFERENCE_RESEARCH.md`：只记录参考项目的公开架构思想和优缺点，不粘贴源代码；
- `docs/CLEAN_ROOM_LOG.md`：记录哪些设计来自公开标准、哪些是本项目独立设计；
- `THIRD_PARTY.md`：列出全部第三方依赖、用途、版本、许可证和是否进入最终产物；
- `docs/PROTOCOL_ORIGINALITY.md`：说明本项目协议与参考项目之间的边界；
- `docs/SECURITY_ASSUMPTIONS.md`：说明安全假设和暂未解决的风险。

发现许可证不清楚、来源不明或具有传染性风险的代码时，不得引入。

---

## 三、参考项目和参考方向

可以研究但只能用于理解架构，不得复制实现：

- Tailscale：控制平面与数据平面分离、节点发现、NAT 穿透、直连优先和中继兜底；
- ZeroTier：虚拟网络抽象、节点身份、路径选择和网络规则；
- Nebula：Lighthouse 思路、证书身份、可扩展覆盖网络和安全组；
- NetBird：集中式设备管理、Setup Key、访问策略、路由发布和审计；
- WireGuard：最小化攻击面、密钥轮换、抗重放和简洁数据平面思想；
- ICE / STUN / TURN：候选地址收集、连接探测、打洞和中继降级；
- Microsoft NetAdapterCx 示例：只用于理解 Windows 虚拟网卡开发流程；
- Linux 内核 TUN/TAP 文档：用于正确使用 `/dev/net/tun`。

研究完成后先产出一份独立设计，不得把参考项目目录结构或协议字段原样照搬。

---

## 四、首版范围

首个可上线版本只做以下范围：

### 4.1 必须支持

- Linux x86_64 / arm64 Agent；
- Windows 10 / Windows 11 x86_64 Agent；
- IPv4 三层虚拟网络；
- Linux TUN；
- Windows 自研虚拟网卡驱动；
- 自研 UDP 加密数据通道；
- 设备间直连；
- NAT 探测与 UDP 打洞；
- 自研中继回退；
- 虚拟 IP 分配；
- 节点和子网路由；
- ACL；
- Web 管理控制台；
- 一次性注册密钥；
- Linux 一键安装脚本；
- Windows 安装包；
- 服务端部署文件；
- 自动升级框架；
- 审计日志和诊断工具。

### 4.2 首版暂不做

- 二层 TAP、ARP 广播和虚拟交换机；
- Android、iOS；
- macOS；
- Kubernetes CNI；
- 公网商业计费；
- 大规模多租户 SaaS；
- 全功能企业 SSO；
- 网络流量内容审计；
- 复杂 SD-WAN 链路聚合；
- 默认把所有互联网流量导向中心节点。

首版必须先把“真实可用的三层内网互通”做稳，不得因为追求功能数量牺牲核心质量。

---

## 五、总体架构

采用“中心控制、分布式传输”的架构。

```text
                        ┌─────────────────────────┐
                        │      XS Controller      │
                        │  身份 / IPAM / ACL / 路由 │
                        │  节点发现 / 配置下发 / 审计 │
                        └───────────┬─────────────┘
                                    │ HTTPS / WSS
                  ┌─────────────────┼─────────────────┐
                  │                 │                 │
            ┌─────▼─────┐     ┌─────▼─────┐     ┌─────▼─────┐
            │  Agent A  │◄───►│  Agent B  │◄───►│  Agent C  │
            │ 100.88.x  │ UDP │ 100.88.x  │ UDP │ 100.88.x  │
            └─────┬─────┘     └─────┬─────┘     └─────┬─────┘
                  │                 │                 │
                  └─────────┐  无法直连时  ┌──────────┘
                            ▼              ▼
                       ┌─────────────────────────┐
                       │       XS Relay          │
                       │  仅转发端到端加密数据包   │
                       └─────────────────────────┘
```

核心组件：

1. `xs-controller`：控制平面；
2. `xs-relay`：自研中继和地址探测服务；
3. `xs-agent`：节点守护进程；
4. `xs-cli`：本地命令行工具；
5. `xs-console`：Web 管理控制台；
6. `xsnet`：Windows 自研虚拟网卡驱动；
7. `xsp-core`：自研覆盖网络协议核心；
8. `xsp-lab`：NAT、丢包、断网和攻击测试实验环境。

---

## 六、推荐技术栈

优先使用：

- 核心服务、Agent、Relay、协议实现：Rust；
- Windows 虚拟网卡驱动：C/C++ + WDK + NetAdapterCx；
- Web 控制台：TypeScript + React；
- 数据库：PostgreSQL；
- 控制通信：HTTPS REST + WebSocket；
- 数据通信：自研 UDP 协议；
- Linux 服务管理：systemd；
- Windows 服务管理：Windows Service；
- 服务端部署：同时提供 systemd 和容器化部署方案。

不得为了统一语言而把复杂逻辑塞入 Windows 内核驱动。Windows 驱动只负责虚拟网卡数据收发，密码学、NAT、会话、策略和中继全部放在用户态 Agent 中。

---

## 七、建议仓库结构

```text
/
├─ apps/
│  ├─ controller/
│  ├─ relay/
│  ├─ agent/
│  ├─ cli/
│  └─ console/
├─ crates/
│  ├─ protocol/
│  ├─ crypto/
│  ├─ identity/
│  ├─ control-client/
│  ├─ dataplane/
│  ├─ nat-traversal/
│  ├─ relay-client/
│  ├─ ipam/
│  ├─ routing/
│  ├─ policy/
│  ├─ telemetry/
│  ├─ updater/
│  └─ platform/
├─ drivers/
│  └─ windows-xsnet/
├─ installers/
│  ├─ linux/
│  └─ windows/
├─ deploy/
│  ├─ systemd/
│  ├─ docker/
│  └─ 1panel/
├─ lab/
│  ├─ namespaces/
│  ├─ nat-scenarios/
│  └─ fault-injection/
├─ docs/
├─ tests/
├─ scripts/
├─ PROGRESS.md
├─ DECISIONS.md
├─ SECURITY.md
├─ THIRD_PARTY.md
└─ README.md
```

---

## 八、身份和密钥体系

### 8.1 节点身份

每个节点首次安装时，在本机生成长期身份密钥，不得由服务器代生成私钥。

节点身份至少包含：

- `node_id`；
- 节点公钥；
- 所属网络；
- 虚拟 IP；
- 节点名称；
- 设备类型；
- 允许的角色和标签；
- 凭证序列号；
- 签发时间；
- 过期时间；
- 控制器签名。

私钥存储要求：

- Windows：使用 DPAPI 或系统安全存储加密；
- Linux：权限必须为 `0600`，并预留 TPM / Secret Service 扩展；
- 日志、诊断包、崩溃转储中不得出现私钥或完整注册令牌。

### 8.2 注册流程

1. 管理员在控制台创建一次性 Enrollment Token；
2. Token 需要设置有效期、最大使用次数、默认分组和默认权限；
3. Agent 本地生成密钥；
4. Agent 向 Controller 提交公钥、设备信息和 Token；
5. Controller 验证后分配虚拟 IP，签发节点凭证；
6. Token 使用后立即失效或减少剩余次数；
7. 数据库只保存 Token 哈希，不保存明文；
8. 注册、吊销、重新签发和密钥轮换全部写入审计日志。

### 8.3 会话密钥

不得自创密码算法。使用标准原语，例如：

- Ed25519：长期身份签名；
- X25519：临时密钥协商；
- HKDF-SHA-256：会话密钥派生；
- ChaCha20-Poly1305 或 AES-256-GCM：数据包 AEAD；
- SHA-256 / BLAKE2：哈希和标识计算。

握手必须包含：

- 双方节点凭证校验；
- 双方临时密钥；
- 握手上下文哈希；
- 双向身份签名或等价认证；
- 网络 ID 和节点 ID 绑定；
- 防降级字段；
- 协议版本；
- 随机数；
- 重放保护。

实现后必须编写 `docs/CRYPTOGRAPHIC_DESIGN.md`，并明确标注哪些部分仍需第三方安全审计。

---

## 九、自研数据平面协议

协议暂定名：**XSP/1（XS Secure Path Protocol v1）**。

### 9.1 协议目标

- 基于 UDP；
- 支持点对点和中继；
- 中继时保持端到端加密；
- 支持多路径探测；
- 支持网络切换；
- 支持密钥轮换；
- 支持抗重放；
- 支持版本升级；
- 避免内核态复杂逻辑；
- 不依赖 WireGuard 数据包格式。

### 9.2 数据包

设计独立二进制包头，至少包含：

- Magic；
- Protocol Version；
- Packet Type；
- Flags；
- Network ID；
- Source Node ID；
- Destination Node ID；
- Session ID；
- Key Epoch；
- 64 位 Sequence；
- Payload Length；
- AEAD Tag。

业务 IP 包整体作为加密负载。

必须：

- 对包头中需要认证的字段使用 AEAD Additional Data；
- 每个方向独立密钥和计数器；
- 实现滑动重放窗口；
- 序列号不得回绕复用；
- 旧 Key Epoch 只保留很短的平滑切换窗口；
- 会话按时间、流量或安全事件轮换；
- 收到伪造源节点、错误虚拟 IP 或越权路由数据包时直接丢弃并记安全事件。

### 9.3 MTU

首版默认虚拟网卡 MTU 使用保守值，建议从 1280 开始，并提供探测和后台配置能力。

首版不应依赖复杂的自定义业务分片。优先通过：

- 合理 MTU；
- Path MTU Discovery；
- TCP MSS 调整；
- ICMP Packet Too Big / Fragmentation Needed；
- 对转发网段做边界检查；

来避免黑洞和碎片问题。

---

## 十、虚拟网卡

### 10.1 Linux

使用 Linux 原生 `/dev/net/tun` 创建三层接口，例如 `xsn0`。

要求：

- 直接使用 TUN 接口；
- 使用 Netlink API 管理接口、IP、路由和状态；
- 不把 `ip`、`route`、`ifconfig` 命令作为运行时核心依赖；
- Agent 异常退出或卸载时恢复其创建的路由；
- 支持 systemd；
- 支持 x86_64 和 arm64；
- 支持服务器和常见 NAS Linux 环境；
- 不接管与虚拟网段无关的本机流量。

### 10.2 Windows

从零开发 `xsnet` 虚拟网卡驱动：

- 基于 WDK 和 NetAdapterCx；
- 优先评估 UMDF，无法满足稳定数据通道时再选择最小化 KMDF；
- 驱动只实现虚拟 NIC 和 Agent 数据交换；
- 用户态 Agent 完成加解密、路由、NAT、策略和状态机；
- 驱动与 Agent 使用受控 IOCTL、共享内存环形缓冲区或其他安全 IPC；
- 做严格长度校验、状态校验和并发保护；
- 禁止把网络输入直接当可信数据；
- 开发阶段提供测试签名；
- 发布阶段预留微软正式签名和兼容性测试流程；
- 安装失败、升级失败和卸载时必须可回滚；
- 必须在虚拟机中执行 Driver Verifier、睡眠恢复、反复安装卸载和异常输入测试。

自研 `xsnet` 驱动源码不得使用 Wintun、TAP-Windows 或复制微软示例代码。可以阅读官方示例理解接口，但实现必须独立完成。首版 Windows 发行可按 2.1 的项目所有者例外，在用户态 Agent 的独立适配器边界使用固定且发行方签名的官方 Wintun `0.14.1`；该例外不改变 `xsnet` 的独立实现、测试签名状态和实机验收要求。

---

## 十一、控制平面

`xs-controller` 负责：

- 用户和管理员认证；
- 网络管理；
- IPAM；
- 节点注册；
- 节点凭证签发和吊销；
- 节点在线状态；
- 节点候选地址交换；
- ACL 和分组；
- 子网路由审批；
- Relay 列表和优先级；
- 配置版本；
- 更新通道；
- 审计日志；
- 告警；
- 诊断信息；
- 控制台 API。

### 11.1 控制连接

Agent 使用 HTTPS 完成注册和初始配置，使用 WebSocket 接收增量配置和节点变化。

要求：

- 全程 TLS；
- 服务端证书必须校验；
- 支持证书固定策略但要有安全轮换机制；
- Agent 断线后指数退避重连；
- 控制器不得保存节点私钥；
- 配置必须带版本号和签名；
- Agent 只接受版本递增且签名正确的配置；
- Controller 短暂不可用时，Agent 使用最近一次有效配置继续转发；
- Controller 恢复后自动增量同步；
- 禁止控制平面承载普通业务数据。

### 11.2 IPAM

默认池为 `100.88.0.0/16`。

要求：

- 地址池可修改；
- 保留基础设施地址；
- 节点地址稳定；
- 删除节点后进入冷却期再复用；
- 检测与本机已有网段、其他 VPN、运营商 CGNAT 地址冲突；
- 冲突时明确提示，不得悄悄覆盖系统路由；
- 支持手动指定地址；
- 支持后续增加 IPv6 ULA 地址池；
- 不允许两个活动节点拥有同一虚拟 IP。

---

## 十二、NAT 穿透

不得使用 coturn 或第三方 STUN/TURN 服务。

实现自研的地址发现和连接探测能力，可以遵循 RFC 8489、RFC 8445 和 RFC 8656 的设计原则，但代码必须独立编写。

### 12.1 候选地址

Agent 收集：

- 本机局域网地址；
- 本机 IPv6 地址；
- 通过我方地址发现服务得到的公网映射地址；
- 管理员配置的静态公网端点；
- Relay 端点。

候选地址通过 Controller 交换，不得把节点私钥交给 Controller。

### 12.2 打洞流程

1. A、B 从 Controller 获取对方候选地址；
2. 双方同时向候选组合发送认证探测包；
3. 对可达路径进行延迟和稳定性测量；
4. 优先级建议为：
   - 同局域网直连；
   - 公网 IPv6 直连；
   - UDP 公网映射直连；
   - 管理员静态端点；
   - Relay；
5. 成功后执行加密握手；
6. 持续维护 NAT 映射；
7. 公网端点变化时重新探测；
8. 网络从 Wi-Fi 切换到有线、手机热点或 VPN 时自动恢复；
9. 对称 NAT 或严格企业防火墙环境下自动使用 Relay；
10. 后台展示当前路径类型、候选端点、延迟、切换原因和最近一次探测结果。

打洞探测包必须认证，防止被用于反射放大攻击。

---

## 十三、自研 Relay

`xs-relay` 只转发已经端到端加密的数据包。

要求：

- Relay 不持有业务会话明文密钥；
- Relay 不解密虚拟 IP 数据；
- 节点必须先认证才能建立 Relay 会话；
- Relay 数据包包含有限路由标识，不暴露不必要信息；
- 限制单节点并发、速率、带宽和队列；
- 防止匿名开放转发；
- 防止反射放大；
- 支持心跳、连接迁移和过期清理；
- 支持多个 Relay；
- Controller 可下发 Relay 区域、优先级、负载和健康状态；
- Agent 定期尝试从 Relay 回切到直连；
- Relay 故障时自动选择其他 Relay；
- 记录流量字节数、延迟、丢包和错误，但不记录业务内容；
- 首个 Relay 部署在成都中心节点。

中继协议由本项目独立设计，不要求完整兼容 TURN。

---

## 十四、路由和真实局域网互通

### 14.1 虚拟网络路由

每个 Agent 必须维护：

- 虚拟 IP 到目标节点的映射；
- 当前活动路径；
- 当前会话；
- ACL；
- 子网路由；
- 路由优先级；
- 配置版本。

### 14.2 子网路由

允许管理员将某个节点设置为 Subnet Router，例如：

```text
节点：成都网关
虚拟 IP：100.88.0.10
发布真实网段：
- 192.168.0.0/24
- 10.170.0.0/24
```

要求：

- Agent 可以自动识别本机局域网，但只能提出建议；
- 未经管理员审批，不得自动发布整个局域网；
- Controller 审批后再向其他节点下发路由；
- 支持启用、暂停和撤销；
- 检测重叠网段；
- 多节点发布同一网段时必须有优先级或拒绝冲突；
- 网关节点必须显式启用转发；
- 对转发流量应用 ACL；
- 防止源地址伪造；
- 网关离线后及时撤销或失效路由；
- 支持 NAT 和纯路由两种模式，但默认优先纯路由；
- 所有系统路由变更都必须可回滚。

### 14.3 后续扩展

预留但首版默认关闭：

- Exit Node；
- 强制全部流量经成都中心节点；
- 多出口策略；
- 按目标网段选择出口；
- DNS 分流；
- 应用级代理。

---

## 十五、ACL 和安全策略

ACL 必须在发送端和接收端同时执行。

策略维度：

- 用户；
- 节点；
- 节点组；
- 标签；
- 源虚拟 IP；
- 目标虚拟 IP；
- 目标子网；
- TCP / UDP / ICMP；
- 端口范围；
- 时间；
- 是否允许作为 Subnet Router；
- 是否允许使用 Relay；
- 是否允许管理操作。

要求：

- 默认拒绝；
- 策略带版本和签名；
- Agent 本地编译成高效匹配结构；
- 控制器失联期间继续使用最近有效策略；
- 新策略下发失败时不得清空旧策略；
- 数据包源地址必须与已认证节点身份匹配；
- 管理控制台可解释某条连接为什么允许或拒绝；
- 所有策略修改写入审计日志。

---

## 十六、管理控制台

控制台使用中文为主，整体风格简洁、专业、偏企业运维，不做花哨动画。

至少包含：

### 16.1 首页

- 在线节点数；
- 离线节点数；
- 直连节点数；
- 中继节点数；
- Relay 健康状态；
- 最近 24 小时流量；
- 连接成功率；
- 平均延迟；
- 待审批路由；
- 安全告警；
- 最近活动。

### 16.2 节点管理

展示：

- 名称；
- 虚拟 IP；
- 系统；
- 架构；
- Agent 版本；
- 公网出口；
- 本地地址；
- 当前路径；
- Relay；
- 延迟；
- 收发流量；
- 最后在线；
- 分组；
- 标签；
- 已发布网段；
- 密钥状态；
- 更新状态。

操作：

- 改名；
- 分组；
- 标签；
- 禁用；
- 删除；
- 吊销凭证；
- 重新注册；
- 下发更新；
- 查看诊断；
- 查看路由；
- 查看连接；
- 允许或禁止作为网关。

### 16.3 网络拓扑

- 展示节点；
- 展示直连和中继链路；
- 展示已发布子网；
- 展示路径延迟；
- 展示故障节点；
- 支持点击查看详情；
- 不得用虚假动画模拟连接状态。

### 16.4 其他页面

- 网络和地址池；
- Enrollment Token；
- 分组和标签；
- ACL；
- 子网路由审批；
- Relay；
- 用户和权限；
- 更新管理；
- 审计日志；
- 告警；
- 系统设置；
- 备份和恢复。

---

## 十七、安装和接入

### 17.1 Linux

控制台生成一次性安装命令，但 Token 不应长期暴露在日志和进程列表。

示例使用体验：

```bash
curl -fsSL https://vpn.xiashikeji.cn/install/linux | sudo bash
```

脚本随后交互输入：

- Controller 地址；
- Enrollment Token；
- 节点名称；
- 是否允许自动启动；
- 是否建议发布本地网段。

也应支持非交互方式，但需要明确安全风险。

安装器必须：

- 检测架构；
- 下载对应二进制；
- 校验哈希和发布签名；
- 创建专用用户；
- 安装 systemd 服务；
- 创建配置目录；
- 生成本机密钥；
- 注册；
- 启动；
- 验证虚拟网卡和控制连接；
- 失败时回滚；
- 输出诊断命令。

### 17.2 Windows

提供正式安装包：

- 安装 Windows Service；
- 安装 `xsnet` 驱动；
- 输入或粘贴 Controller 地址和 Enrollment Token；
- 生成本机密钥；
- 注册；
- 创建虚拟网卡；
- 配置虚拟 IP 和路由；
- 支持静默安装；
- 支持完整卸载；
- 卸载时删除虚拟网卡和本项目路由，但保留可选诊断日志；
- 驱动和 Agent 版本必须兼容；
- 升级失败自动回滚。

---

## 十八、升级系统

实现自研升级框架：

- Controller 管理稳定、测试和开发通道；
- 发布清单包含版本、平台、架构、哈希、大小和签名；
- Agent 使用独立离线更新签名公钥验证；
- 禁止仅依赖 HTTPS 而不校验发布签名；
- 支持分批发布；
- 支持暂停；
- 支持强制最低版本；
- 支持回滚；
- Windows 驱动升级需要单独处理；
- 升级过程不得破坏节点身份；
- 升级失败后自动恢复旧版本；
- 更新签名私钥不得保存在 Controller 在线服务器。

---

## 十九、可靠性要求

必须处理：

- Controller 重启；
- Relay 重启；
- Agent 重启；
- 网络断开恢复；
- DHCP 地址变化；
- 公网 IP 变化；
- NAT 映射变化；
- Wi-Fi / 有线切换；
- 系统睡眠和唤醒；
- 时钟偏差；
- 数据库短暂不可用；
- 配置重复下发；
- 配置乱序；
- 节点被删除后仍保持旧会话；
- 证书过期；
- 密钥轮换；
- 多 Relay 故障；
- 路由冲突；
- Windows 驱动升级失败；
- Linux Agent 异常退出；
- 磁盘满；
- 日志过大；
- 恶意畸形数据包。

任何失败都不得导致系统悄悄把默认路由改错或把无关流量导入隧道。

---

## 二十、日志、监控和诊断

日志要求：

- 结构化日志；
- 支持级别调整；
- 自动轮转；
- 默认不记录业务数据内容；
- 默认不记录 Token、Cookie、私钥和完整会话密钥；
- 公网 IP 在导出诊断包时可脱敏；
- 每条错误包含稳定错误码；
- 前端错误可对应后端诊断信息。

指标至少包括：

- 节点在线率；
- 控制连接重连次数；
- 直连成功率；
- Relay 使用率；
- 握手耗时；
- 路径延迟；
- 丢包；
- 抗重放丢弃数；
- ACL 丢弃数；
- 收发字节；
- 路由数量；
- 配置版本；
- 更新状态。

CLI 至少支持：

```text
xs status
xs peers
xs ping <virtual-ip>
xs path <virtual-ip>
xs routes
xs netcheck
xs diagnostics
xs reconnect
xs version
```

---

## 二十一、安全测试

建立明确威胁模型，至少覆盖：

- Controller 被攻击；
- Relay 不可信；
- 某个节点被攻陷；
- Enrollment Token 泄露；
- 中间人攻击；
- 重放攻击；
- 伪造源节点；
- 伪造虚拟 IP；
- 恶意路由发布；
- 配置回滚；
- 密钥降级；
- UDP 反射放大；
- Relay 滥用；
- 恶意驱动输入；
- 恶意控制台管理员；
- 软件更新供应链攻击。

必须测试：

- 畸形包 Fuzz；
- 握手状态机 Fuzz；
- 包长度和整数溢出；
- 重放窗口；
- 随机丢包；
- 乱序；
- 延迟；
- 重复包；
- MTU 黑洞；
- NAT 映射超时；
- Controller 假配置；
- 过期证书；
- 被吊销节点；
- Token 重复使用；
- ACL 绕过；
- 子网路由绕过；
- Windows Driver Verifier；
- Linux 权限边界；
- 升级包篡改；
- 数据库恢复。

不得只写测试文件而不实际运行。测试报告需要记录命令、结果和环境。

---

## 二十二、实验室环境

创建 `lab/`，通过 Linux network namespace、虚拟网卡和防火墙规则模拟：

- 同局域网；
- 两端普通 NAT；
- 端口受限 NAT；
- 对称 NAT；
- 仅一端有公网 IP；
- UDP 被封锁；
- 高延迟；
- 丢包；
- 乱序；
- Relay 故障；
- Controller 故障；
- 多出口切换。

测试必须证明：

1. 能直连时使用直连；
2. 不能直连时使用 Relay；
3. Relay 恢复或直连条件变化后能重新选路；
4. 控制器断开不会立即中断已有会话；
5. 被吊销节点会在策略允许的最短窗口内失去访问权限；
6. 子网路由只在审批后生效；
7. 卸载 Agent 后系统路由恢复正常。

---

## 二十三、阶段划分

### 阶段 0：研究和设计

产出：

- 架构文档；
- 威胁模型；
- 协议草案；
- 密钥体系；
- 状态机；
- 数据库模型；
- API 草案；
- Windows 驱动方案；
- 测试矩阵；
- Clean-room 记录。

完成后立即进入实现，不要停下来只等待确认。

### 阶段 1：Linux 最小真实链路

实现：

- Controller；
- Linux Agent；
- Linux TUN；
- 节点注册；
- IPAM；
- 两节点 UDP 加密通信；
- 基础 CLI；
- 基础实验室测试。

验收：

- 两个 Linux 节点可通过 `100.88.0.0/16` 虚拟 IP 双向 ping；
- 业务包确实经过自研加密通道；
- 抓取公网 UDP 流量看不到原始业务 IP 包；
- Controller 重启后已有会话继续工作。

### 阶段 2：NAT 和 Relay

实现：

- 地址发现服务；
- 候选交换；
- UDP 打洞；
- 路径探测；
- 自研 Relay；
- 直连与 Relay 自动切换；
- 网络变化恢复。

验收：

- NAT 实验矩阵通过；
- 同一对节点能展示当前是 Direct 还是 Relay；
- Relay 无法解密业务数据；
- Relay 故障后能切换。

### 阶段 3：Windows

实现：

- `xsnet`；
- Windows Agent；
- Windows Service；
- 安装包；
- 路由配置；
- Windows 与 Linux 互通；
- 睡眠恢复和网络切换。

验收：

- Windows 与 Linux 可通过虚拟 IP ping、SSH、SMB 或 HTTP；
- 驱动反复安装卸载不残留；
- 测试签名流程有文档；
- Driver Verifier 测试无崩溃；
- Agent 崩溃不会导致系统网络失效。

### 阶段 4：子网路由、ACL 和控制台

实现：

- 子网识别；
- 路由审批；
- 子网转发；
- ACL；
- 完整控制台；
- 审计；
- 诊断；
- Relay 管理。

验收：

- 虚拟节点可通过批准的网关访问真实局域网；
- 未批准的网段无法访问；
- ACL 拒绝规则确实阻断；
- 路由冲突能被检测；
- 网关离线后路由失效。

### 阶段 5：部署和硬化

实现：

- Linux 安装脚本；
- Windows 安装器；
- 自动升级；
- 回滚；
- 备份恢复；
- 1Panel 部署说明；
- 性能测试；
- 安全测试；
- 发布文档。

验收：

- 可在成都服务器部署 Controller 和 Relay；
- 新 Linux 节点可通过一条安装命令加入；
- 新 Windows 节点可通过安装包加入；
- 完整安装、升级、卸载流程通过；
- 最终文档可由其他工程师独立部署。

---

## 二十四、Codex 工作方式

你不是只负责输出方案，而是负责在当前工作区持续完成实现。

必须遵守：

1. 先检查当前环境和工具链；
2. 初始化 Git 仓库；
3. 创建 `PROGRESS.md`、`DECISIONS.md` 和任务清单；
4. 每完成一个可验证里程碑就运行测试；
5. 测试通过后再提交 Git；
6. 提交信息清晰说明功能；
7. 不得伪造测试结果；
8. 不得用 Mock 替代最终网络链路验收；
9. 不得留下大量只有 `TODO` 的空壳；
10. 遇到一般性选择时自行做安全、可维护的决定；
11. 遇到无法在当前环境完成的事项，先完成其余部分，并提供可复现的阻塞证据；
12. 不得为了赶进度削弱安全边界；
13. 修复问题时先定位根因，不做无关大范围重写；
14. 所有重要设计决策记录到 `DECISIONS.md`；
15. 所有命令、端口、配置、密钥路径和部署步骤写入文档；
16. 所有生成的默认密码必须改为安装时随机生成；
17. 不得把密钥、Token、真实服务器密码提交到 Git；
18. 最终代码必须能从干净环境构建。

每一阶段结束时更新：

- 已完成功能；
- 测试结果；
- 未完成事项；
- 已知风险；
- 下一阶段；
- 当前可运行命令。

---

## 二十五、最终交付物

最终必须交付：

- 完整源代码；
- Controller 可执行程序；
- Relay 可执行程序；
- Linux Agent；
- Windows Agent；
- Windows 虚拟网卡驱动；
- Linux 安装脚本；
- Windows 安装包；
- Web 控制台；
- PostgreSQL 数据库迁移；
- 1Panel / systemd / 容器部署文件；
- 协议规范；
- API 文档；
- 安全设计；
- 威胁模型；
- Clean-room 文档；
- 第三方依赖和许可证清单；
- 测试报告；
- 性能报告；
- 安装、升级、卸载和灾难恢复文档；
- `FINAL_REPORT.md`。

`FINAL_REPORT.md` 必须真实说明：

- 哪些功能已完成；
- 哪些功能只完成部分；
- 哪些功能未完成；
- 已运行哪些测试；
- 测试结果；
- 当前安全风险；
- Windows 驱动签名状态；
- 当前是否适合生产使用；
- 部署所需服务器端口；
- 后续优先级。

不得把未实现能力描述为已完成。

---

## 二十六、首版验收场景

最终至少完成以下真实场景：

### 场景 A：Windows 与 Linux

- Windows 节点加入网络；
- Linux 服务器加入网络；
- 两者分处不同公网出口；
- 通过虚拟 IP 双向访问；
- 控制台显示路径、延迟和流量。

### 场景 B：无公网 IP 的 NAS

- NAS 加入网络；
- 不开放公网入站端口；
- Windows 通过虚拟 IP 访问 NAS 服务；
- 能直连则直连，不能直连自动 Relay。

### 场景 C：真实局域网访问

- 成都网关发布 `192.168.0.0/24`；
- 管理员审批；
- 其他虚拟节点访问该局域网；
- 未授权节点被 ACL 阻止；
- 成都网关离线后路由自动失效。

### 场景 D：控制器故障

- 已连接节点正在传输；
- 停止 Controller；
- 已建立的数据通道继续；
- 新配置暂不生效；
- Controller 恢复后 Agent 自动重连并同步。

### 场景 E：中继故障

- 两节点处于 Relay；
- 当前 Relay 停止；
- Agent 自动切换其他 Relay或重新尝试直连；
- 连接恢复过程有日志和状态展示。

---

## 二十七、立即开始

现在直接执行以下动作：

1. 检查工作区和开发环境；
2. 初始化仓库；
3. 建立上述目录；
4. 编写阶段 0 文档；
5. 确定 XSP/1 的独立协议草案；
6. 建立 Controller、Agent、Relay 的最小可编译骨架；
7. 首先打通两个 Linux network namespace 之间的真实 TUN 加密通信；
8. 运行测试并记录；
9. 持续进入后续阶段，不要停留在计划层面。

优先级始终是：

> **真实互通 > 安全正确 > 稳定恢复 > 可观测性 > 管理体验 > 功能数量。**
