{
  "name": "xs-nexus",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/console"
  ],
  "scripts": {
    "build": "npm run build --workspace @xs-nexus/console",
    "lint": "npm run lint --workspace @xs-nexus/console",
    "test": "npm run test --workspace @xs-nexus/console"
  }
}
