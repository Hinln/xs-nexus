# AGENTS.md — 拾枢（XS Nexus）仓库总规则

本文件是 Codex 在本仓库中的长期行为规范。进入任何任务前，必须先读取本文件、`XS_Nexus_Codex_Development_Brief.md`、`EXECUTION_PLAN.md` 和 `PROGRESS.md`。

---

## 1. 规则优先级

发生冲突时依次遵守：

1. 安全边界、禁止事项和真实凭据保护；
2. `ACCEPTANCE.md` 中的强制验收；
3. `XS_Nexus_Codex_Development_Brief.md` 的产品和架构约束；
4. 目录级 `AGENTS.md`；
5. `EXECUTION_PLAN.md`；
6. 当前实现细节。

不得以进度、方便、兼容性或“先跑起来”为理由违反更高优先级规则。

---

## 2. 工作目标

你负责从当前仓库状态持续实施整个系统，而不是只输出建议、脚手架、演示页面或概念验证。

持续目标包括：

- 真实 Linux TUN 数据通路；
- 自研 XSP/1 加密 UDP 数据平面；
- 控制器、节点、Relay；
- NAT 探测和打洞；
- 路由、ACL、IPAM；
- Windows 自研虚拟网卡驱动；
- Web 控制台；
- 安装、升级、回滚；
- 网络和故障实验；
- 缺陷闭环；
- 视觉、交互和可访问性验收；
- Release Candidate 与完整文档。

---

## 3. 自主执行规则

普通技术选择不询问用户。选择安全、稳定、可测试、可维护的方案，并记录到 `DECISIONS.md`。

每次工作循环：

1. 读取 `PROGRESS.md`、Git 状态和最近提交；
2. 找到最早的未完成强制里程碑；
3. 检查现有实现，避免重复和回退；
4. 先写或补充失败测试；
5. 完成真实实现；
6. 运行格式化、静态分析、编译、单测、集成测试和适用的端到端测试；
7. 修复失败；
8. 更新状态和证据；
9. 创建范围单一的 Git 提交；
10. 立即进入下一项。

不得只创建空目录、大量 TODO、永远通过的 Mock 或静态假数据后宣布完成。

---

## 4. 禁止依赖

核心运行路径不得依赖、调用、封装或改名使用：

- Tailscale / Headscale；
- WireGuard 可执行程序、内核模块或 Wintun；
- ZeroTier；
- NetBird；
- Nebula；
- OpenVPN；
- SoftEther；
- FRP、rathole、nps；
- coturn；
- TAP-Windows；
- 其他现成组网、VPN、穿透或中继产品。

不得复制上述项目源代码、私有协议、包格式、密钥格式、目录结构或品牌元素。

允许使用操作系统原生 API、公开标准、通用框架和标准密码学库。

---

## 5. 密码学规则

- 不自创密码算法；
- 只使用成熟库中的标准原语；
- 不实现自己的 ChaCha20、AES、Ed25519、X25519、HKDF；
- 协议组合必须有清晰的 transcript、身份绑定、版本绑定和防降级；
- 每个方向使用独立密钥、序列号和重放窗口；
- 密钥不得记录到日志；
- 中继不得获得业务明文密钥；
- 未完成第三方安全审计前，不得将自研协议描述为“已达到生产级安全”。

对协议或密码学做出修改时，必须同步更新设计文档、测试向量和威胁模型。

---

## 6. 凭据和数据保护

禁止把真实秘密写入：

- Git；
- Markdown 状态文件；
- Shell 历史；
- Dockerfile；
- Compose 文件；
- 测试快照；
- 前端；
- 日志；
- 截图；
- 诊断包；
- 崩溃转储；
- 构建产物。

真实配置只允许存在于仓库外、权限受控的环境文件或 Secret 中。

运行命令时避免把密码直接写在命令行参数。优先读取文件描述符、环境文件或交互输入。

---

## 7. 1Panel 和服务器保护

不得：

- 删除、重建或修改 `1panel-network`；
- 删除未知 Docker 容器、镜像、网络或卷；
- 执行 `docker system prune -a`；
- 修改 1Panel 数据库；
- 修改无关网站；
- 覆盖 1Panel Nginx/OpenResty 全局配置；
- 关闭系统防火墙；
- 破坏 SSH 管理端口；
- 修改默认路由而不设置自动回滚；
- 暴露 PostgreSQL、Redis 或 MySQL 到公网。

网络实验必须优先放进独立 namespace。

影响宿主机网络的操作必须：

1. 保存当前状态；
2. 准备自动回滚；
3. 保留 SSH；
4. 限制变更范围；
5. 执行后验证；
6. 记录恢复命令。

---

## 8. Docker 与宿主机边界

Docker 部署：

- Controller；
- Relay；
- Console；
- Worker；
- Playwright QA；
- 其他普通用户态服务。

宿主机 systemd：

- `xs-agent`；
- TUN；
- Netlink 路由；
- namespace 实验协调器；
- 需要真实宿主机网络能力的测试。

普通服务容器默认非 root。不得为了方便把所有容器设置为 `privileged`。

---

## 9. Git 纪律

- 初始化 Git；
- 每个可验证里程碑单独提交；
- 提交前运行适用测试；
- 不提交构建缓存、秘密、数据库数据、截图临时文件或驱动私钥；
- 不重写已有历史，除非仓库尚未形成共享历史且确有必要；
- 不使用含糊提交信息；
- 不将多个无关重构和功能混在一个提交；
- 最终 Git 状态必须干净。

提交信息建议：

```text
feat(agent): add Linux TUN packet loop
fix(relay): reject unauthenticated session allocation
test(protocol): cover replay-window boundary
docs(decision): record host-agent deployment choice
```

---

## 10. 测试纪律

禁止：

- 删除失败测试；
- 使用 `skip`、`ignore`、`xfail` 隐藏核心失败；
- 降低断言；
- 把真实网络测试替换成 Mock；
- 伪造测试报告；
- 把“编译成功”当成产品验收；
- 把一次偶然通过当成稳定性结论。

测试结果必须记录：

- 命令；
- 时间；
- 环境；
- 结果；
- 产物或日志路径；
- 已知限制。

所有 Bug 修复必须尽量先建立复现测试。

---

## 11. UI 规则

控制台页面必须使用真实后端或明确标识的测试夹具，不得用永远正常的硬编码状态掩盖错误。

每个页面都必须覆盖：

- 正常状态；
- 加载状态；
- 空状态；
- 错误状态；
- 权限不足；
- 长文本；
- 大量数据；
- 窄屏；
- 键盘导航。

视觉验收必须使用浏览器真实渲染和截图，不能只读源码后自我判定。

---

## 12. Windows 驱动规则

Windows 驱动是高风险模块。必须遵守目录级 `drivers/windows-xsnet/AGENTS.md`。

不得：

- 在开发服务器上伪造 Windows 驱动实机结果；
- 把编译通过描述成 Driver Verifier 通过；
- 在用户日常电脑上进行首轮驱动测试；
- 绕过驱动签名和安装安全检查；
- 把协议、加密、ACL 或 NAT 放进内核。

---

## 13. 上下文恢复

`PROGRESS.md` 是唯一恢复入口之一。每个阶段、重大中断或长上下文前必须更新：

- 当前提交；
- 当前里程碑；
- 已完成；
- 正在进行；
- 下一条准确命令；
- 最近测试；
- 已知失败；
- 临时环境；
- 恢复方法。

不得依赖聊天上下文保存关键事实。

---

## 14. 阻塞处理

只有外部条件才算阻塞，例如：

- 缺少 Windows 测试机；
- 缺少 WDK；
- 缺少正式驱动签名；
- 缺少用户执行 NAS 安装；
- 缺少 DNS 或防火墙人工授权。

遇到阻塞：

1. 写入 `BLOCKERS.md`；
2. 附证据；
3. 写清解除步骤；
4. 完成其他不受影响的工作；
5. 不得把阻塞项标记完成。

---

## 15. 完成定义

只有 `ACCEPTANCE.md` 的全部强制项目有实际证据、`RELEASE_CHECKLIST.md` 通过、`FINAL_REPORT.md` 如实完成，才允许将项目状态标记为 Release Candidate。

不得自行使用“正式生产可用”“密码学已安全”“驱动已正式签名”等未经证据支持的表述。
