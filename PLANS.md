# PLANS.md — 执行计划维护规范

`EXECUTION_PLAN.md` 是持续更新的活文档，不是一次性列表。任何能够持续超过一个工作循环的任务，都必须反映到计划中。

---

## 1. 计划必须回答的问题

每个里程碑必须明确：

- 为什么要做；
- 前置条件；
- 具体范围；
- 不在本里程碑中的内容；
- 要修改的组件；
- 验收标准；
- 实际验证命令；
- 证据路径；
- 失败和恢复方法；
- 是否存在人工门禁。

---

## 2. 状态值

仅使用：

- `NOT_STARTED`
- `IN_PROGRESS`
- `BLOCKED_EXTERNAL`
- `FAILED`
- `PASSED`
- `DEFERRED_APPROVED`

不得使用模糊状态，如“基本完成”“大概可用”“差不多”。

---

## 3. 里程碑模板

```markdown
## Mx.y 名称

- 状态：
- 负责人：Codex
- 前置：
- 风险等级：
- 人工门禁：

### 目标

### 范围

### 非范围

### 实施步骤

### 验收标准

### 验证命令

### 证据

### 失败恢复

### 实际结果
```

---

## 4. 计划更新规则

- 开始里程碑时改为 `IN_PROGRESS`；
- 发现设计变化时先更新计划和 `DECISIONS.md`；
- 完成后写实际命令和证据，才能改为 `PASSED`；
- 外部阻塞写入 `BLOCKERS.md`，状态改为 `BLOCKED_EXTERNAL`；
- Bug 不允许在计划中悄悄消失；
- 发生失败时记录原因，修复后保留历史；
- 不得为了显示进度拆成大量无意义小任务；
- 不得把文档创建本身当成功能完成。

---

## 5. 计划和 Git

每个里程碑结束时：

1. 运行验证；
2. 更新计划；
3. 更新 `PROGRESS.md`；
4. 更新已知问题；
5. 提交 Git；
6. 在计划中记录提交哈希。

---

## 6. 验收证据

可接受证据：

- 自动化测试输出；
- 网络抓包摘要；
- 日志；
- Playwright 截图；
- 性能报告；
- 安装和卸载记录；
- 迁移和回滚记录；
- Driver Verifier 报告；
- 哈希和签名验证；
- Git 提交。

不可接受：

- “代码看起来正确”；
- “理论上可以”；
- 只存在 Mock；
- 没有运行过的测试脚本；
- 手工修改后未记录；
- 截图和测试环境不对应。

---

## 7. 计划恢复

每次进入仓库时：

1. 读取 `PROGRESS.md`；
2. 查看 `git status`；
3. 查看最近 10 个提交；
4. 读取当前 `IN_PROGRESS` 里程碑；
5. 重新运行最小验证；
6. 再继续实现。

如果状态文件和代码不一致，以代码、测试和 Git 证据为准，并修正状态文档。
