# IMPLEMENT.md — 无人值守实施手册

---

## 1. 初次启动

按顺序执行：

1. 阅读全部根目录规则文件。
2. 检查工作区是否为空或已有部分实现。
3. 查看系统、Docker、网络和磁盘。
4. 不修改 1Panel 资源，先生成基线。
5. 初始化 Git。
6. 创建项目结构。
7. 将 `PROGRESS.md` 当前里程碑设为 M0.1。
8. 开始实施，不停留在计划层面。

---

## 2. 每轮自治循环

```text
恢复上下文
→ 确定最早未完成里程碑
→ 读取相关代码和目录规则
→ 建立或运行失败测试
→ 最小实现
→ 格式化和静态分析
→ 单元测试
→ 集成测试
→ 端到端测试
→ 检查日志和副作用
→ 修复
→ 更新状态和文档
→ Git 提交
→ 下一里程碑
```

---

## 3. 实现原则

- 先打通最小真实纵向链路，再扩功能；
- 核心协议优先可审计和简单；
- 内核代码最小化；
- 网络输入全部不可信；
- 控制器不进入业务数据通路；
- Relay 不解密业务数据；
- 配置必须有版本、签名和回滚保护；
- 所有系统网络变更必须可恢复；
- 后台展示真实状态而非推测状态；
- 每个功能同时考虑失败路径。

---

## 4. 推荐实施顺序

### 纵向链路 1

```text
节点注册
→ IPAM
→ Linux TUN
→ 两节点 UDP
→ 握手
→ 加密业务包
→ Ping
→ Controller 离线保持
```

### 纵向链路 2

```text
候选地址
→ 路径探测
→ UDP 打洞
→ Relay
→ 故障切换
→ Direct 回切
```

### 纵向链路 3

```text
策略
→ ACL
→ 子网路由
→ 网关离线
→ 冲突检测
```

### 纵向链路 4

```text
控制台
→ 真实 API
→ Playwright
→ 视觉修复
```

### 纵向链路 5

```text
安装
→ 升级
→ 回滚
→ 卸载
→ 干净重装
```

---

## 5. 构建基线

项目形成后，至少提供统一入口：

```bash
make fmt
make lint
make build
make test
make test-unit
make test-integration
make test-network
make test-e2e
make test-visual
make security-check
make release
```

如果不用 Make，可使用 `just` 或仓库脚本，但必须有一个清晰统一入口。

---

## 6. 测试失败处理

发现失败：

1. 保存原始日志；
2. 最小化复现；
3. 创建 Bug 编号；
4. 添加回归测试；
5. 找根因；
6. 修复；
7. 运行受影响测试；
8. 运行完整回归；
9. 更新 Bug 和状态；
10. 提交。

不得通过延长睡眠、无限重试、忽略错误码或降低断言来掩盖竞态。

---

## 7. 状态写入

每完成一个工作单元，都更新：

- `PROGRESS.md`
- `DECISIONS.md`（有设计变化时）
- `KNOWN_ISSUES.md`
- `BLOCKERS.md`（有外部阻塞时）
- `THIRD_PARTY.md`（有新依赖时）

---

## 8. 长上下文处理

上下文即将过长时，不要停下询问用户。先写入：

- 当前提交；
- 当前未提交修改；
- 下一条命令；
- 最近测试结果；
- 当前根因判断；
- 临时服务和端口；
- 清理命令；
- 回滚命令。

然后从 `PROGRESS.md` 恢复继续。

---

## 9. 开发环境与 RC 隔离

开发：

- 调试日志；
- 测试数据；
- 开发端口；
- 可热重载；
- 可重建。

Release Candidate：

- 从干净提交构建；
- Release 模式；
- 固定依赖；
- 单独数据目录；
- 单独容器名称；
- 正式迁移；
- 不使用开发密钥；
- 可回滚。

不得把开发数据库、测试 Token 或测试节点带入 RC。

---

## 10. 停止条件

只有以下两类情况可以停止：

1. `ACCEPTANCE.md` 和 `RELEASE_CHECKLIST.md` 已满足，已产出真实 `FINAL_REPORT.md`；
2. 所有剩余工作都被不可绕过的外部阻塞，且 `BLOCKERS.md` 已写完整证据和解除方法。

一般 Bug、编译错误、测试失败、依赖冲突和架构选择不属于停止理由。
