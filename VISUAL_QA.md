# VISUAL_QA.md — 控制台视觉、交互与可访问性验收

---

## 1. 目标

Web 控制台应呈现简洁、专业、可信的企业网络运维产品，不使用夸张动画和虚假实时效果。

视觉验收必须基于真实浏览器渲染、真实 DOM、真实网络请求和截图。

---

## 2. 固定分辨率

至少：

- 1440 × 900；
- 1920 × 1080；
- 1280 × 720；
- 1024 × 768；
- 768 × 1024；
- 390 × 844。

控制台若明确不支持手机完整管理，也必须保证手机尺寸可阅读、可提示使用桌面端，不能页面完全错乱。

---

## 3. 页面矩阵

每页覆盖：

- 加载；
- 正常；
- 空；
- 错误；
- 无权限；
- 大量数据；
- 长名称；
- 长 IPv6；
- 长错误信息；
- 离线；
- 部分服务异常。

页面：

- 登录；
- 首页；
- 节点列表；
- 节点详情；
- 网络；
- 地址池；
- Enrollment Token；
- 分组和标签；
- ACL；
- 子网审批；
- Relay；
- 拓扑；
- 用户和角色；
- 更新；
- 审计；
- 告警；
- 系统设置；
- 备份恢复；
- 404/500。

---

## 4. 检查项目

### 布局

- 无水平滚动条（明确设计除外）；
- 表格不压坏页面；
- 弹窗不超屏；
- 下拉菜单不被裁切；
- 固定栏不遮挡；
- 卡片间距统一；
- 按钮高度统一；
- 表单标签对齐；
- 空状态不误导。

### 内容

- 中文为主；
- 技术名词一致；
- Direct、Relay、Offline 有明确中文解释；
- 时间、流量、延迟单位统一；
- 不显示 `undefined`、`null`、内部堆栈；
- 不显示真实秘密；
- 错误信息可操作。

### 交互

- 所有按钮可点击；
- 禁用状态有原因；
- 提交防重复；
- 删除有确认；
- 高风险操作要求输入确认；
- Toast 不遮挡关键操作；
- 表单错误聚焦；
- Loading 不永久卡住；
- 失败可重试。

### 可访问性

- 键盘可达；
- 焦点可见；
- Label 正确；
- 对比度合理；
- 图标有文本或 accessible name；
- 表格语义；
- Dialog 焦点锁定和恢复；
- 页面标题；
- 不只用颜色表达状态。

---

## 5. Playwright 要求

测试必须：

- 固定浏览器版本；
- 固定测试数据；
- 等待字体和网络稳定；
- 屏蔽随机时间戳或使用稳定夹具；
- 保存失败截图、视频、trace；
- 检查 Console；
- 检查未解释 4xx/5xx；
- 检查主流程；
- 检查权限；
- 检查响应式。

截图命名：

```text
artifacts/visual/<commit>/<viewport>/<page>/<state>.png
```

---

## 6. 视觉修复循环

```text
启动真实环境
→ 重置固定数据
→ Playwright 遍历
→ 保存截图和 trace
→ 分类问题
→ 修复最高影响问题
→ 运行相关页面
→ 运行完整视觉回归
→ 更新记录
```

---

## 7. 禁止

- 只根据源代码宣布视觉正确；
- 用截图占位替代页面；
- 通过隐藏溢出掩盖布局；
- 删除错误状态；
- 使用假节点状态；
- 对所有视觉差异盲目更新基线；
- 因测试不稳定而放宽断言；
- 在截图中暴露真实 Token、IP 密钥或密码。

---

## 8. 通过条件

- 所有强制页面已覆盖；
- 所有固定分辨率运行；
- 无 P0/P1 视觉或交互问题；
- 无关键元素遮挡；
- 无未解释 Console 错误；
- 主流程可键盘完成；
- 截图证据完整；
- 基线变化有人工可读说明。
